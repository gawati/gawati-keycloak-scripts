{
  "name": "gawati-kc-realm-generator",
  "private": true,
  "version": "1.0.0",
  "description": "Generates KeyCloak Realms from a model JSON exported KeyClock realm",
  "main": "index.js",
  "dependencies": {
    "express": "^4.16.3",
    "yargs": "^11.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "build": "mkdir build && cp -arv index.js LICENSE build/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "vars": "env | grep npm_package_ | grep -v npm_lifecycle_script"
  },
  "author": "",
  "license": "ISC"
}
